{
  "name": "nitheky-proof",
  "version": "1.0.0",
  "description": "NITHEKY same-direction matching — technical proof: route corridor, direction validation, atomic last-seat reservation. Zero runtime dependencies.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/*.test.ts\"",
    "build": "esbuild web/entrada.ts --bundle --format=esm --platform=browser --minify --outfile=motor.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
